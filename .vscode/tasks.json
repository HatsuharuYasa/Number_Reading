{
    "version": "2.0.0",
    "tasks":[
        {
            "label": "build",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build",
                "--config",
                "Debug"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
        },
        {
            "label": "cmake configure",
            "command": "cmake",
            "args":[
                "-S",
                "${workspaceFolder}",
                "-B",
                "${workspaceFolder}/build"
            ],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        },
        {
            "label": "configure and build",
            "dependsOn": ["cmake configure", "build"],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}